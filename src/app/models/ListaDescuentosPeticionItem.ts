export class ListaDescuentosPeticionItem {
    idPeticion: string;
    tipo: string; //1 (anticipo) , 2 (monedero)
    desTipo: string;
    importe: number;
    descripcion: string;
    idAnticipo: string;
}